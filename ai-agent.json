{
  "enabled": false,
  "schema": "drivecanvas.diagnostic-agent.v3",
  "endpoint": "",
  "note": "Runtime AI endpoint is activated only after the server-side OpenAI deployment is verified. No OpenAI API key is ever stored here or in the APK."
}
